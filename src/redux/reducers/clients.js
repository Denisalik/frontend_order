const initialState = {
    clients: []
}

export const reducer = (state=initialState, action) => {
    switch(action.type){
        //case _.CLIENTS:
        // return state.set("clients", action.payload);
    }
}